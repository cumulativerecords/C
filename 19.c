/* Using Bool from C99*/
#include <stdio.h>
#include <stdbool.h>

int main ()

{
	int j;
	bool k;
	printf("Enter a number:\n");
	scanf("%d", &j);
	
	//now assign value of int to the bool
	k = j;
	// Use bool to check if int j is non-zero.
	 k;

	return 0;
}
