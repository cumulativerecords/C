/* check equivalence
 * running test input "10.3 5 6"
 * getting control of the output
 */
#include <stdio.h>

int main()
{
	int i, j;
	float x;
	scanf("%d%f%d", &i, &x, &j);
	printf("%d %.1f %d", i, x, j);

	return 0;
}
