/* check equivalence
 * running test input "12.3 45.6 789"
 * getting control of the output
 */
#include <stdio.h>

int main()
{
	int i;
	float x, y;
	scanf("%f%d%f", &x, &i, &y);
	printf("%f %d %f", x, i, y);

	return 0;
}
